# MoWE agent bootstrap

This repository uses a compact active-context layer plus searchable history.

Before changing code or project documents:

1. Read `CODEX_PROJECT_RULES.md`.
2. Follow its minimal startup protocol.
3. Treat root `IMPLEMENTATION_PLAN.md` as the current execution contract.
4. Read `DEV_LOG.md` current snapshot and latest entries.
5. Inspect `git status --short` and preserve all unrelated/uncommitted work.

Do not load `docs/history/` files in full during startup. Search them with `rg` and read only the matching section when exact historical rationale, commands, errors, or evidence are needed.

Current default direction: `flow_wam_skill_moe`; finish the LIBERO formal feature-store/readiness/training/evaluation chain before CALVIN. Never present mock, local-contract, or historical evidence as a current real GPU/simulator result.
